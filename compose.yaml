services:
  spam-bot:
    build:
      context: .
    image: fuckyou-spam-rust:local
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped
    stop_grace_period: 30s
